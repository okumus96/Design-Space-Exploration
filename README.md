# Design-Space-Exploration
sudo ip link add name gurobihack type dummy
sudo ip link set dev gurobihack address 00:00:4a:00:51:89
sudo ip link set gurobihack up




1) Use real software architectures (autoware) to be able say it is real
2) Add addtional latency penalty on hop in terms of latency


#SECONDARY
3) We can add vendor compatibility.
4) Think about Energy consumption
5) Check whether load balancing causes more ECUs or not.
6) Consider ASIL decomposition